namespace Foundry.Modules.Workers.Features.Login;

// Minimal stub for step 1 — step 2 adds Parse(json), loggedIn/email/orgName/subscriptionType fields.
internal sealed record AccountIdentity(string Email);
